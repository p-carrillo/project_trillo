{
  "name": "@trillo/modules",
  "version": "0.1.0",
  "private": true,
  "main": "dist/platform/main.js",
  "types": "dist/platform/main.d.ts",
  "scripts": {
    "dev": "tsx watch platform/main.ts",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fastify": "^5.2.1",
    "mysql2": "^3.12.0"
  }
}
